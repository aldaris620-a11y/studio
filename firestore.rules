
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    function isOwner(userId) {
      return request.auth != null && request.auth.uid == userId;
    }

    match /users/{userId} {
      // Allow read, delete, and write (create/update) if the user is the owner.
      // This is a robust way to handle profiles, as it allows creation if the doc
      // doesn't exist, and updates if it does.
      allow read, delete, write: if isOwner(userId);
                      
      match /game_progress/{gameProgressId} {
        allow read, write: if isOwner(userId);
      }
      
      match /user_rewards/{userRewardId} {
        allow read, write: if isOwner(userId);
      }
    }

    match /rewards/{rewardId} {
      allow read: if request.auth != null;
    }
  }
}

    